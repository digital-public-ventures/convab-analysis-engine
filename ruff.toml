target-version = 'py313'
line-length = 120
extend-exclude = ['temp']

[format]
quote-style = 'single'

[lint]
select = ['E', 'W', 'F', 'I', 'B', 'C4', 'UP']
ignore = ['E501']

[lint.per-file-ignores]
'*/tests/*' = ['S101', 'PLR2004', 'PLC0415']
